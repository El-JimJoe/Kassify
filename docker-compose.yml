services:
  kassify:
    build: .
    image: ghcr.io/el-jimjoe/kassify:latest
    container_name: kassify
    ports:
      - "8084:80"
    restart: unless-stopped
    volumes:
      - kassify-data:/data
      # Unraid-Beispiel: - /mnt/user/appdata/kassify:/data
    environment:
      KASSIFY_CORS: "*"

volumes:
  kassify-data:
